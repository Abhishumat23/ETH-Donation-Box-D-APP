{
  "name": "donation-box-dapp",
  "version": "1.0.0",
  "description": "A beginner-friendly blockchain donation box dApp using Ethereum and Hardhat",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "test-contract": "npx hardhat run scripts/test-contract.js --network localhost",
    "node": "npx hardhat node",
    "start": "cd frontend && python3 -m http.server 8080"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "solidity",
    "hardhat",
    "dapp"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "3": "^2.1.0"
  }
}
